import React from 'react';

import { Stack, Typography } from '@mui/material';

const AddWegit = () => (
  <Stack>
    <Stack>
      <Typography variant="h3">Create Query</Typography>
    </Stack>
  </Stack>
);

export default AddWegit;
